[project]
name = "py-common"
version = "0.0.1"
description = "personalized-reco Python 공유 유틸 (DB·설정·지표)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/py_common"]

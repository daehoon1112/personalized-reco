[project]
name = "pipelines"
version = "0.0.1"
description = "personalized-reco ML/데이터 배치 파이프라인 (컨슈머는 apps/bronze-sink, Kotlin)"
requires-python = ">=3.12"
dependencies = [
    "py-common",
    "psycopg[binary]>=3.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
reco-pipelines = "pipelines.cli:main"

[tool.uv.sources]
py-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pipelines"]
